//test file for CMPM 120 git gud assignment

var Dude(game, key) {

Phaser.Sprite.call(this, game, 32, game.height - 32, key);

//set anchor to lower right in this version
this.anchor.set(1, 1);

var catchphrase = 'Pineapple does belong on pizza';

console.log(catchphrase);
}
